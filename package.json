{
  "name": "ytigdown",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --check worker.js && node scripts/check-inline-scripts.cjs",
    "bump": "node scripts/bump-version.cjs",
    "version:check": "node scripts/bump-version.cjs --check",
    "predeploy": "npm test && npm run bump"
  },
  "devDependencies": {}
}
